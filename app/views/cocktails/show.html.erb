<%= render 'shared/navbar' %>
<div><%= render 'shared/banner' %> </div>
<div class="container">
    <%# <div class="card-product">
      <%# <% <div class="card-product-photo"> %> 
      <%#= cl_image_tag @cocktail.photo.key, height: 300, width: 400, crop: :fill %>
  <%# </div> %>
  <div class="card-product-infos">
    <div class="card-product-description">
      <h1><%= @cocktail.name %></h1>
      <% @cocktail.doses.each do |dose| %>
        <h1> Made of  <%= dose.description %></h1>
        <h2>you need <%= dose.ingredient_name %> </h2> 
        <%= link_to "Add A Dose", new_cocktail_dose_path(@cocktail), class: "btn btn-outline-primary" %>
        <%= link_to "Delete A Dose", dose_path(dose), method: :delete, class:"btn btn-outline-warning"  %>
        <%= link_to 'Back to List', cocktails_path , class: "btn btn-outline-danger" %>
      <% end %>
    </div>
   </div>
 </div>
</div>


<!-- not completed -->

